# taru — PLAN (pointer)

The plan lives in the Obsidian vault, where Hannu noodles on it:
**`~/penny-one/projects/writing/taru/PLAN.md`**

Read it before any design work — its `## Operator directives` section is law. Project notes live in the same folder (`~/penny-one/projects/writing/taru/`, INDEX.md + PLAN.md + whatever Hannu adds).

In an instance without that vault (a fork — see README §Your own instance), this file itself is the plan: `./setup.sh` resets it to a blank one with an operator-directives section of its own.
