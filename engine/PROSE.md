# PROSE.md — Transcript → Prose (a seam, not yet a tool)

The pipeline this file reserves: scenes played in taru become draft prose in Hannu's style.

**Contract.** Input: one or more scene files + the world's canon + a style module. Output: `worlds/<world>/drafts/NNN-slug.md`. The transcript is compost, not quotation — re-dream the scene in the style; keep the events, the canon, and every `⭐`-marked line verbatim.

**The style module does not exist yet.** It will come from `~/fable/style-lab` (the authorial-voice discriminator project); when it lands, drop it in as `engine/style/<name>.md` and reference it here. Until then, "prose" answers: *the seam is ready; the style module isn't.*
